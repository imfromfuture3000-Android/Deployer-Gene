const web3 = require('@solana/web3.js');
require('dotenv').config(); const spl = require('@solana/spl-token'); async function deployMainnet() { console.log('?? OMEGA PRIME MAINNET DEPLOYMENT'); const connection = new web3.Connection('${process.env.HELIUS_API_KEY ? `https://mainnet.helius-rpc.com/?api-key=${process.env.HELIUS_API_KEY}` : (process.env.RPC_URL || "https://api.mainnet-beta.solana.com")}`', 'confirmed'); const mint = web3.Keypair.generate(); console.log('WARNING: MAINNET DEPLOYMENT - REAL SOL REQUIRED'); console.log('Mint:', mint.publicKey.toBase58()); console.log('You need a funded mainnet wallet to continue...'); console.log('This script will fail without real SOL balance'); return { mint: mint.publicKey.toBase58(), network: 'mainnet-beta', status: 'ready for real wallet' }; } deployMainnet().catch(console.error);
